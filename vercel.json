{
  "version": 2,
  "installCommand": "corepack enable && corepack prepare pnpm@9.12.0 --activate && pnpm i --frozen-lockfile",
  "buildCommand": "pnpm --filter @fancytrader/shared build && pnpm -C apps/frontend build",
  "outputDirectory": "apps/frontend/build",
  "framework": "vite"
}
