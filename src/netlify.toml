[build]
  command = "npm install && npx vite@5.4.11 build"
  publish = "build"
  
[build.environment]
  NODE_VERSION = "18"
  NPM_FLAGS = "--legacy-peer-deps=false"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
